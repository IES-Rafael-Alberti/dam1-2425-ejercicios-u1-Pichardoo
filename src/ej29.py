import random

# Genera un número entero aleatorio entre min_val y max_val
min_val = 1
max_val = 15
numero_aleatorio = random.randint(min_val, max_val)

print(f"Número entero aleatorio entre {min_val} y {max_val}: {numero_aleatorio}")
 